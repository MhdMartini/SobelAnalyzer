sobelAnalyzer:

read image
apply different levels of noise - 4
for each noise level:
    apply soble
    try a range of thresholds, calc accuracy of each, and return the best threshold with its accuracy
